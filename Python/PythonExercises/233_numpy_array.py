import numpy as np

amount_numbers = int(input("How many numbers should be in the array: "))
user_array_zero = np.zeros(amount_numbers)
print(user_array_zero)

user_array_ones = np.ones(amount_numbers)
print(user_array_ones)
