events {
}

http {

    include mime.types;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html/;

        location /welcome/sergii {
            return 200 "Hello!";
        }

    }
}

